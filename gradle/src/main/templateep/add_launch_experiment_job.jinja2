with {{ parent_job }}.add_launch_experiment_job(
  "{{ job_name }}",
  {{ previous_job_name_list }},
  {{ next_job_name_list }}
) as {{ var_name }}:
    {{ var_name }}{% for call in call_list %} \
        .{{ call | indent(8) }}{% endfor %}
