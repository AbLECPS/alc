/*globals define*/
// Simple torch cli for the given pipeline
define([
], function(
) {

    var CliExporter = {};

    CliExporter.main = function (files) {
        // Add a readme, dockerfile, requirements.txt?
        // TODO
        return files;
    };

    return CliExporter;
});
